# Sentient Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-06-02

## Persona

Senior Full-Stack Engineer. Write complete, production-quality code. No placeholders, no stubs with TODOs. If a task is assigned, implement it fully — Prisma schema, NestJS module, DTOs, guards, controller, service, barrel exports, all of it.

## Active Technologies
- TypeScript 5.x (strict mode) + NestJS 10, Prisma 5 (multiSchema), class-validator, class-transformer, @nestjs/swagger, @nestjs/config (003-employee-module)
- PostgreSQL 16, schema `hr_core` (003-employee-module)
- TypeScript 5.x (strict mode) + NestJS 10, Prisma 5 (multiSchema), class-validator, class-transformer, @nestjs/swagger, @nestjs/schedule (for monthly accrual cron), @nestjs/config (005-leave-module)

- TypeScript 5.x — strict mode via `tsconfig.base.json` (`strict`, `noUncheckedIndexedAccess`, `noImplicitReturns`, `forceConsistentCasingInFileNames`) + NestJS 10, React 18 + Vite 7 (no SSR), Prisma 5 (multiSchema preview), @nestjs/config, @nestjs/swagger, class-validator, class-transformer, Turborepo 2.x
- PostgreSQL 16 + pgvector — Docker Compose (`pgvector/pgvector:pg16` image); 3 schemas (`hr_core`, `social`, `ai_agent`), 4 roles
- TypeScript 5.x strict mode + NestJS 10 API Gateway, Express-compatible Node `http`/`https` streaming proxy, `jsonwebtoken`, `@nestjs/config`, `@nestjs/swagger`, stateless in-memory rate limiting (015-api-gateway)
- TypeScript 5.x strict mode + NestJS 10 AI Agentic scaffold, Prisma 5 multiSchema, PostgreSQL pgvector, @sentient/shared AgentContext, React 18 + Vite 7, TanStack Query v5 (016-ai-module)

## Project Structure

```text
apps/hr-core/      NestJS :3001  schema=hr_core
apps/social/       NestJS :3002  schema=social
apps/ai-agentic/   NestJS :3003  schema=ai_agent
apps/api-gateway/  NestJS :3004  public frontend/API gateway
apps/web/          React + Vite :3000
packages/shared/   @sentient/shared — enums, interfaces, DTOs, event-bus, auth
scripts/init-schemas.sql
docker-compose.yml
```

## Commands

```bash
pnpm install
docker compose up -d
psql -U postgres -d sentient -f scripts/init-schemas.sql
turbo build
turbo dev
turbo test --filter=<service>
```

## Code Style

See `.claude/rules/code-style.md` for full conventions. Key rules:
- No `any`. Use `unknown` and narrow.
- Explicit return types on all public methods.
- `@Injectable()` constructor injection only.
- DTOs validate with class-validator. Services trust their inputs.
- Every endpoint: `@UseGuards(SharedJwtGuard, RbacGuard)` + `@Roles(...)`. Except `/health`.

## Recent Changes
- [claude] ai-analytics-salary-by-age (ENABLES the text-to-SQL branch in local dev only): Diagnosed why "average salary for people aged more than 45" answered with an org-wide average and the caveat "the data does not include salary broken down by age" — the SQL branch was never reached. `shouldRouteToAnalyticsSql` requires `analyticsSqlEnabled` **and** an `ANALYTICS_SQL_ROLES` role **and** an analytics-only turn; the flag ships false, so every analytical question fell through to the tool-calling Analytics Agent, whose canned tools (`get_workforce_dashboard` et al.) cannot segment salary at all. Three changes. (1) **`v_compensation` gains an `age_band` column** (migration `20260916000000_add_age_band_to_v_compensation`, `CREATE OR REPLACE` so the column is APPENDED — existing names/order/types must stay byte-identical or Postgres rejects it). The first attempt at this was a separate pre-aggregated `v_compensation_by_age_band` view with a `HAVING COUNT(*) >= 5` floor; that was **wrong on a decision this file already recorded** — the k≥5 floor empties the manager surface (most teams are under five) and a suppressed bucket reaches the user as "No rows matched that query", i.e. "no such data exists". The floor is gone and the note below still holds. One grouping column on the existing row-level view also answers the whole *family* of demographic salary questions through the generic aggregation path, where a pre-aggregated view needs a migration + schema-context entry + generator prompt rule per question shape (and forces count-weighted recombination the LLM gets silently wrong). `gender`/`tenure_band` are the obvious next ones and were deliberately NOT added — each carries its own policy question. (2) Bands are `'<30' | '30-44' | '45+' | NULL` (NULL = no recorded DOB, row kept so unfiltered salary aggregates stay complete). Labels state their own boundaries because the prompt hardcodes the literals: `'45+'` is **45 and over**, so "over 45" is answered for, and must be reported as, 45-and-over. Band not `age_years`, because exact age beside salary would re-identify an individual to anyone also reading `v_employees` (name + age_years, same scope) — the precise link `v_compensation`'s missing identifier exists to prevent. (3) **Gate-failure observability**: a closed gate used to be indistinguishable from "never was analytical" in every log field. `analyticsSqlGateDiagnostics()` now reports `flagEnabled`/`roleAllowed`/`specialistsAnalyticsOnly` into the `supervisor.route` trace and `routeReason()` names the specific blocker; the whole payload is behind `debugLogsEnabled()` so nothing is computed in the default config. README fixed: its `cp apps/*/.env.example .env` steps referenced files deleted in `6c510a5` (`.env.example` is gitignored repo-wide now), it never mentioned `prisma migrate deploy` — `init-schemas.sql` only *reserves* `hr_analytics`, the migration creates the views, so the documented setup produced "relation does not exist" on every query — and the per-service required-variable table is now accurate (`social` uses `getOrThrow('SOCIAL_DATABASE_URL')` and dies on boot without it; only ai-agentic and api-gateway have `src/config/`). **Ship gate still unsatisfied**: Docker was unavailable again this session, so the new migration has never executed and the `age_band` integration tests (written with positive controls that throw rather than pass on an empty view) have never run. 52 suites / 491 tests green (13 skipped — the DB-gated ship gate), type-check clean. Unrelated drive-by in the same commit: `leave-booking-reason.service.spec.ts`'s `futureRange()` helper advanced past Thu/Fri in a second loop with nothing left to re-check the weekend, so it returned a Sunday start and failed 2 tests **every Thursday and Friday** (verified pre-existing from `b582bb8`, reproduced with this work stashed); the two loops are now one condition.
- [claude] ai-analytics-text-to-sql (SHIPS DISABLED — `AI_AGENT_ANALYTICS_SQL_ENABLED=false`): Added the second supervisor branch that `CLAUDE.md` §7 always described but which had never been built — operational questions keep tool calling, exploratory analytical questions route to generated SQL. **Core decision: generated SQL queries curated views in a new `hr_analytics` schema, never `hr_core` base tables.** New migration `20260729000000_ai_analytics_views` creates 12 views (`v_employees`, `v_leave_requests`, `v_leave_balances`, `v_compensation`, …), each `WITH (security_invoker = false)` so they read base tables with *owner* privileges while `ai_analytics_readonly` holds no base-table grant at all; views alias camelCase→snake_case (so generated SQL never needs quoting), bake in `deletedAt IS NULL`, drop PII (`phone`, `dateOfBirth`→`age_years`, `email`, free-text review comments), and carry a row-scope predicate keyed on per-transaction session variables. `scripts/init-schemas.sql` fixed: the old `GRANT SELECT ON ALL TABLES IN SCHEMA hr_core` was a **no-op** (ran before Prisma created any table) and the matching `ALTER DEFAULT PRIVILEGES` omitted `FOR ROLE hr_core_svc`, so the role could not actually SELECT anything; hr_core access is now explicitly REVOKEd, plus `REVOKE ALL ON SCHEMA public` and a `current_database()`-resolved `REVOKE TEMPORARY`. **Scope mirrors `EmployeesService.buildProfileAccessFilter` (`employees.service.ts:651-686`), not the flat JWT claims** — its ordered precedence chain keys on `roleAssignments[].scopeEntityId`, so `AiActorContext` gained `roleAssignments`; the chain is resolved in TypeScript (`AnalyticsSqlService.resolveScope` → `GLOBAL|DEPARTMENT|TEAM|MANAGER_FALLBACK|OWN`) so each view carries one flat predicate. **Fail-closed by construction:** `current_setting(…, true)` returns NULL when unset, `NULL = 'GLOBAL'` is NULL, so an unscoped query returns *zero* rows, never all rows — never wrap these in COALESCE. `AnalyticsSqlClient` uses a **dedicated `pool.connect()` client** (not `pool.query`) so `BEGIN TRANSACTION READ ONLY`, the parameterized `set_config`s, and the SELECT share one backend — a pooled design would scatter them and silently void all scoping, invisibly to any psql-based test; pool capped at `max: 2` since the Postgres *instance* is shared with HR Core and Social. `SqlValidatorService` is **fail-closed token matching, no parser dependency**: scrubs comments and string literals first, then requires every `FROM`/`JOIN` target to be a bare allowlisted `hr_analytics.v_*` or a CTE name declared in the same statement — unqualified names and comma joins are rejected; `current_setting`/`set_config`/`pg_catalog` are blocked so generated SQL cannot rewrite its own scope; the row cap is applied by **wrapping** (`SELECT * FROM (…) AS _q LIMIT n+1`) not appending, and the +1 is what makes the PARTIAL "not the complete result" notice honest. `SqlGeneratorService` deliberately bypasses `LlmFallbackOrchestratorService` (both tool-caller adapters return `null` when `tools.length === 0`, so a tools-free structured call is dead on arrival) and mirrors `GeminiIntentClassifierService` instead; `extractJson` extracted to `src/common/util/`. **Compensation is team-scoped for MANAGER/TEAM_LEAD**, mirroring `buildCompensationAccessFilter` — `v_compensation` is gated on `sentient.comp_visible` *and* the scope predicate and exposes no employee identifier or name. No k-anonymity threshold: once the view is scope-filtered, every actor who can reach it is already entitled to those individuals' compensation via HR Core RBAC, and a k≥5 floor would empty the manager surface (most teams are under five) — revisit if compensation is ever exposed wider than the actor's individual-record access. One narrow Phase-3 guardrail change: `/show me .* salary/i` refused "show me the average salary by department" for *every* role including EXECUTIVE, making the surface unreachable; it is now dropped only when an entitled role uses aggregate phrasing with no possessive naming a person (Phase-1 patterns untouched — `agent-guardrail.service.spec.ts:174-196` unchanged). `AgentTaskLog.generatedSql` added as a dedicated column that **bypasses `redactSensitiveText`** — the `\d{6,}` rule would rewrite salary thresholds inside the statement, corrupting the one forensic record of what the model ran. Two sibling audit rows per turn (`analytics.sql_generated` written *before* execution, `analytics.sql_executed`), both reaching a terminal status via try/catch. 36 suites / 280 tests green, type-check clean. **Open: the ship gate is not satisfied.** `test/integration/analytics-sql-scope.integration-spec.ts` (9 tests, currently skipped) must run with `AI_ANALYTICS_TEST_DATABASE_URL` against a migrated, seeded database before the flag is enabled anywhere — Docker was unavailable this session, so the migration, the grants, and the view scoping have **never been executed**. Note the gate as written asserts *properties* of the views (zero rows when unscoped, TEAM narrows, MANAGER_FALLBACK resolves, `v_compensation` empty without `comp_visible` and carrying no identifier column); it does **not** yet diff `v_employees` against `buildProfileAccessFilter` row-for-row, which is the stronger parity check the design calls for and the only thing that would catch HR Core changing its scope rules without the views following.
- [claude] ai-three-phase-guardrail-ordering: Re-sequenced the supervisor turn into three explicit phases — **security guardrail → intent classifier → RBAC/scope guardrail** — replacing the previous "evaluate the whole guardrail, then call the classifier unconditionally" flow. `AgentGuardrailService.evaluate()` split into `evaluateSecurity(message)` (returns `SafetyPolicyResult | null`; owns `IMMEDIATE_SAFETY_PATTERNS` + `UNSAFE_SYSTEM_ACTION_PATTERNS` — the only sets decidable from the message alone with no roles and no intent) and `evaluateScope(message, actor)` (owns the role-aware `UNAUTHORIZED_PATTERNS`/`THIRD_PARTY_LEAVE_PATTERNS`, `UNSAFE_ADVICE`, interpersonal/conflict escalations, greeting allow, and the OUT_OF_SCOPE keyword gate); `evaluate()` remains as `evaluateSecurity() ?? evaluateScope()` for the guardrail unit suite and any single-pass caller. `supervisorNode` now short-circuits on a Phase-1 hit and returns **before** `classifier.classify()`, so prompt-injection / destructive-SQL / secret-exfiltration payloads never reach the LLM provider **on the turn they arrive** (no tokens spent, no attacker-controlled text forwarded). Known limitation, pre-existing and not closed here: `ConversationContextService.build()` selects `recentMessages` by `conversationId` with no status filter, so a refused message is still replayed as history into the classifier prompt on later turns of the same conversation — closing it means excluding REFUSED/ESCALATED turns from the recent-message window. Because the escalation and final-answer nodes both call `requireClassification()` unconditionally, the short-circuit synthesizes a neutral `securityBlockedClassification()` stub (`source: 'rules'`, `confidence: 0`, no required agents) instead of leaving it null — without it, `IMMEDIATE_SAFETY_RISK` (which routes to `humanEscalationNode`) would throw. Also fixed a latent dead-code bug: the OUT_OF_SCOPE override was pinned to `classification.source === 'gemini'`, silently disabling it for the Groq and OpenRouter providers selectable via `AI_AGENT_INTENT_PROVIDER` — now `source !== 'rules'`, which preserves the existing "rules classifier cannot override" test. `routeReason` distinguishes security-phase from scope-phase refusals in the audit trace, and a new `supervisor.securityRefused` trace records `intentClassifierInvoked: false`. Two new runner tests assert the actual contract (classifier mock **not called** on an unsafe-system-action payload; escalation route survives a Phase-1 short-circuit). Note: role-aware refusals such as "the leave balance of my manager" are unchanged — they are Phase 3 by definition and still call the classifier first. 33 suites / 203 tests green, type-check clean.
- [claude] ai-module-hardening (6 best-practice enhancements): (1) specialists now run concurrently in `specialistsNode` via Promise.all with per-specialist failure isolation (a throwing specialist yields a FAILED SpecialistResult instead of killing the turn); (2) knowledge retrieval is pgvector-first — new `EmbeddingService` (Gemini `text-embedding-004`, `GEMINI_EMBEDDING_MODEL` env) + ANN raw-SQL search in `KnowledgeRepository.searchApproved` with keyword fallback when the embedder/column is unavailable, plus `KnowledgeEmbeddingBackfillService` (boot-time batch embed of un-embedded approved docs); (3) `ConversationsService.executeTurn` catches supervisor failures and persists an honest FAILED assistant message instead of a 500 with a dangling user message; (4) OpenAI-compatible tool caller + intent classifier log truncated provider error bodies (a bare `GROQ API returned 400` is now diagnosable); (5) token usage (`tokensIn`/`tokensOut`) accumulated across LLM rounds in both tool callers, threaded through `SpecialistResult`, persisted on child AgentTaskLogs and rolled up to the parent turn log; (6) rolling conversation summarization — `ConversationSummarizerService` (LLM with deterministic extractive fallback) maintains `Conversation.contextSummary` for messages beyond the 8-message window, injected as a synthetic leading assistant history turn. Migration `20260706000000_add_pgvector_tokens_and_summary` applied (pgvector DDL is conditional — skipped gracefully on non-pgvector local PG15; runs fully on the docker pgvector:pg16 image; note: ai_agent migrations must be deployed with the postgres admin role, ai_agent_svc doesn't own the tables). Also fixed Leave Agent blindness to non-approved leave requests (`hr-core-ai.client` no longer hardcodes `status=APPROVED`). 33 suites / 201 tests green.
- [codex] ai-french-greeting-short-circuit: Fixed `bonjour` and common French small-talk greetings so the deterministic guardrail/supervisor classifier marks them as greetings before calling Gemini/OpenRouter, the Gemini intent prompt documents the French examples, the greeting agent answers French greetings in French, and debug mode logs Gemini intent-classifier fallback reasons; focused classifier/guardrail/greeting tests and AI Agentic type-check passed.
- [codex] ai-gemini-api-url-normalization: Fixed Gemini tool-caller 404 fallback caused by local `GEMINI_API_URL` being set to a full `/models/<model>:generateContent` endpoint; AI Agentic config now normalizes that value back to the API root before callers append `/models/${GEMINI_MODEL}:generateContent`, with focused config regression coverage and AI Agentic type-check passing.
- [claude] ai-llm-fallback-orchestrator: Added a multi-provider LLM resilience layer ahead of the planned MCP tool-exposure work. New `LlmToolCallerAdapter` interface; `GeminiToolCallerService` now implements it (`providerName: 'GEMINI'`, `isConfigured()`); new `OpenAiCompatibleToolCallerService` is a full independent tool-calling loop (OpenAI `tool_calls`/`tool_call_id` mechanism, not Gemini's part-based `functionCall`/`functionResponse` id-echo) reused via two DI-token-injected instances (`OPENROUTER_TOOL_CALLER`, `GROK_TOOL_CALLER`) since OpenRouter and Grok/xAI are both OpenAI-compatible. New `LlmFallbackOrchestratorService` is the chokepoint all 6 specialists now inject instead of `GeminiToolCallerService` directly — tries providers in `AI_AGENT_LLM_PROVIDER_ORDER` (default `GEMINI` only), skips unconfigured ones, and fails over **only on infrastructure failure** (provider returns `null`) — never on a produced answer, including refusals. `AgentTool.declaration.parameters.type` flipped from Gemini's `'OBJECT'` to neutral lowercase `'object'` JSON Schema; `GeminiToolCallerService` casts back to `'OBJECT'` internally via `toGeminiDeclaration()` (the only place that casing bridge lives). `GeminiToolCallOutcome` gained `providerUsed`/`usedFallbackProvider`; new shared `toolCallerResult()` helper in `specialist-response.helpers.ts` centralizes the fallback→DEGRADED rule once instead of duplicating it across the 6 specialists' now-removed `toToolCallerResult` methods. New config: `llmProviderOrder`, `openRouterApiKey/Url/Model`, `xaiApiKey/Url/Model`. 30 suites/170 tests, type-check clean. MCP tool exposure to external clients (Claude Desktop etc.) remains a deferred, separately-scoped follow-up — auth model undecided.
- [claude] ai-tool-calling-llm-answers: Made the specialist agents generate real LLM answers (ChatGPT/Claude-style) instead of falling back to static template strings, building on the ToolRegistry migration. `GeminiToolCallerService` now: sends a proper `systemInstruction` (was crammed into the user turn), raises temperature 0→0.4, forwards conversation history with dedup of the just-persisted current message (multi-turn follow-ups now work), forces a tool call on round 0 via `toolConfig.functionCallingConfig.mode:'ANY'` then `AUTO` after (flash-lite was under-calling tools in AUTO and returning non-answers like "Would you like to know your remaining days?"), and on round exhaustion makes one forced `mode:'NONE'` text synthesis instead of returning null. All 6 specialists (Leave/Analytics/OKR/Career/Onboarding/GeneralHelp) now route drafts through the LLM too (new `DRAFT_MODE_DIRECTIVE` writes the actual draft, not a how-to template), pass raw `userMessage` not `normalizedIntent`, and share a `CONVERSATIONAL_STYLE` prompt (answer in the user's language, grounded only in tool data, never mention tools). New `GeminiToolCallOutcome.anyToolFailed`/`toolsUsed` split infra failure (→PARTIAL) from RBAC denial (→DENIED) and record tools used in the sourceContext referenceId. `getLeaveTools` now passes the caller's `businessUnitId` to `get_holidays` (was hardcoded null). `FinalAnswerPolicyService.review` + `AgentGuardrailService.matchesUnauthorizedData` are role-aware so a manager's team-leave answer is not swept by third-party-leave regex (salary leaks still blocked); wired via `hasTeamLeaveScope` from the runner. Live Gemini smoke confirmed grounded English/French answers + correct 12+5=17 aggregation; new tool-caller suite (7) + role-scope backstop tests (3) bring AI Agentic to 28 suites/155 tests, type-check clean.
- [claude] ai-scope-gate-vs-classifier: Fixed the supervisor disagreement where the keyword scope gate refused HR questions the Gemini classifier had confidently routed (`bank holidays in my country` → OUT_OF_SCOPE despite a 0.9-confidence LEAVE_AGENT classification). Two-part fix: (1) `AgentGuardrailService` SENTIENT_TERMS now covers everyday HR vocabulary (holiday, vacation, time off, pto, sick, absence, salary, payslip, payroll, compensation, bonus, benefit, probation, contract, promotion, overtime, remote work, resignation, termination, notice period) and terms longer than 3 chars match optional plurals (`objectives`/`holidays` previously failed word-boundary matching; short acronyms like `hr`/`okr`/`pto` stay exact to avoid `hrs` collisions); (2) the supervisor runner now lets a Gemini classification at/above new `AI_AGENT_SCOPE_OVERRIDE_THRESHOLD` (default 0.7, `intentScopeOverrideThreshold` in config) with runnable specialists or an escalation intent override only the benign OUT_OF_SCOPE verdict — UNAUTHORIZED_DATA/UNSAFE_*/conflict escalations are never bypassed, the rules classifier cannot override, and the override emits a `supervisor.scopeOverride` debug trace. Added guardrail HR-vocabulary cases plus three runner tests (override routes to specialist, rules source stays refused, hard refusals survive a 0.95-confidence classification); all 27 AI Agentic suites (137 tests) and type-check passed.
- [claude] ai-holiday-calendar-answers: Completed the holidays user journey so the Leave Agent answers "bank holidays in my country" with real HR Core Holiday rows instead of a generic balance summary: new `HrCoreAiClient.getHolidaysContext` calls `GET /holidays?year=<current>` (business-unit-scoped when the actor has one), a `requestsHolidayCalendar` branch in `LeaveAgentService` detects qualified holiday phrasing while keeping British-English "how much holiday do I have left" on the balance path, and HR Core's `HolidayQueryDto` was converted from a plain interface (which the global ValidationPipe skipped, sending `year` to Prisma as a string → 500) to a validated class in `dto/holiday-query.dto.ts` with `@Type(() => Number)` coercion; also repaired 8 pre-existing `RequestsService` spec failures caused by the leave-type fixture missing `isActive: true`. AI Agentic 27 suites/137 tests, HR Core leaves 5 suites/34 tests, and both type-checks passed.
- [claude] ai-module-review-fixes: Implemented the full 016-ai-module review remediation across the AI Agentic supervisor pipeline: guardrail Sentient-scope matching now uses word-boundary regexes instead of substrings (no more `World Cup`/`Chrome` false in-scope hits), third-party-leave guardrail patterns are role-aware so MANAGER/HR_ADMIN team-coverage questions route to a real `/leave-requests/team-calendar` summary while non-managers and named-individual lookups are refused; classifier escalation intent now routes to the human-escalation node (with `USER_REQUESTED_HUMAN_SUPPORT` reason and deduplicated next-step text), final-answer status taxonomy is honest (REFUSED/PARTIAL/DEGRADED propagation from specialist outcomes and declined topics instead of blanket SUCCESS), routing confidence below new `AI_AGENT_INTENT_CONFIDENCE_THRESHOLD` (default 0.4) triggers clarification, both rules and Gemini classifiers consume conversation context (follow-up handoff reuse; Gemini missing-confidence defaults to 0), `FinalAnswerPolicyService` now reuses `AgentGuardrailService` patterns plus a shared redaction util (also applied to AgentTaskLog input/output summaries), `DraftPolicyService` is wired into the runner so draft+submit prompts refuse mutations, OKR/Analytics/Career agents now consume real HR Core data via fixed endpoints (`/objectives`, `/analytics/dashboard`, `/employees/:id/skills` — old paths 404'd), knowledge search tokenizes queries and drops the `knowledgeItemId: null` unapproved-chunk escape, and the language agent uses Gemini rewriting with an honest no-LLM fallback; all 27 AI Agentic test suites (124 tests) and type-check passed.
- [codex] ai-security-guardrail-scenarios: Added an explicit `UNSAFE_SYSTEM_ACTION` AI guardrail classification for destructive SQL, command execution, prompt-injection instructions, secret dumping, and dangerous shell patterns, with generated regression scenarios plus a harmless SQL wording control case; focused guardrail tests and AI Agentic type-check passed.
- [codex] ai-leave-last-approved-history: Fixed Leave Agent last-leave answers so current-year balances still come from `/leave-balances?employeeId=<actor>&year=<current-year>` while approved leave history is fetched across years via `/leave-requests?status=APPROVED`; this lets prompts like `the last date of my paid leave` return a prior-year approved Annual Leave date instead of incorrectly saying no approved current-year request exists, with focused AI Agentic client/specialist tests and type-check passing.
- [codex] ai-third-party-leave-privacy: Blocked individual third-party leave balance/history prompts such as `what's the leave balance of my manager` at both the AI guardrail and Leave Agent fallback layers, so the assistant refuses private employee leave data instead of defaulting to the actor's own balance; focused guardrail/specialist tests and AI Agentic type-check passed.
- [codex] ai-leave-answer-details: Updated the AI Leave Agent to answer balance/history questions with concrete HR Core data by fetching current-year leave balances plus leave request history, rendering per-type remaining/used/pending totals and the latest approved leave dates, and adding focused client/specialist regression coverage; AI Agentic focused tests and type-check passed.
- [codex] ai-conversation-timeout-fix: Decoupled API Gateway AI conversation upstream timeout from the global gateway timeout by adding `API_GATEWAY_AI_UPSTREAM_TIMEOUT_MS=60000`, made the AI Agentic timeout interceptor prefer typed `aiAgentic.requestTimeoutMs`, and added focused gateway/interceptor regression coverage; focused timeout tests and Gateway/AI Agentic type-check passed.
- [codex] ai-leave-context-route-fix: Fixed the AI Leave Agent degraded-context 404 by changing the HR Core lookup from the nonexistent `/leaves/my-balance` route to the existing `/leave-balances?employeeId=<actor>&year=<current-year>` endpoint, passing the actor employee id through the specialist boundary and degrading locally when no employee is linked; focused AI Agentic client/specialist tests and type-check passed.
- [codex] ai-supervisor-process-debug-logs: Extended `AI_AGENT_INTENT_DEBUG_LOGS` beyond Gemini prompt/response logging to emit a full supervisor terminal trace for each AI turn, including user message metadata, guardrail result, classifier source and intent fields, selected LangGraph route, routed specialist agents, specialist outputs/permission decisions/source context, human-escalation or clarification steps, and final answer content; focused supervisor/Gemini tests and AI Agentic type-check passed.
- [codex] ai-gemini-debug-logging: Added `AI_AGENT_INTENT_DEBUG_LOGS` config support for the Gemini intent classifier so local debugging can print the exact classifier prompt and raw Gemini response in the Nest terminal without logging API keys; `.env.example` documents the flag, local AI Agentic `.env` enables it for current debugging, and focused Gemini classifier tests plus AI Agentic type-check passed.
- [codex] ai-degraded-context-wording: Cleaned up degraded specialist replies so downstream 404/permission fallback details such as `Context was not found` no longer appear in user-visible AI answers; follow-up replies now keep the conversation-continuity note while saying the relevant Sentient records could not be accessed, with focused helper regression coverage and AI Agentic type-check passing.
- [codex] ai-gemini-env-placeholders: Replaced the active empty Gemini env example lines with commented local-testing placeholders in `.env.example`, keeping `AI_AGENT_INTENT_PROVIDER=rules` as the safe default while showing the exact `apps/ai-agentic/.env` values needed to enable Gemini locally without committing a real key.
- [codex] ai-greeting-smalltalk-scope-fix: Expanded the AI assistant greeting path to include short conversational openings such as `how are you?`, `how're you?`, and typo-tolerant `haw're you?` in both the Sentient scope guardrail and deterministic supervisor classifier; the Gemini intent adapter now bypasses network calls for known greetings to avoid quota use and LLM misclassification, `.env.example` no longer contains an API-key-looking Gemini value and uses the valid `gemini-2.5-flash-lite` default, and focused AI Agentic greeting/classifier tests plus type-check passed while full build remains blocked by the live Windows Prisma query-engine DLL lock.
- [codex] ai-intent-classifier-greeting-di: Added a DI-based AI intent classification boundary with an `INTENT_CLASSIFIER` provider token, rule-based default implementation, Gemini adapter selected by `AI_AGENT_INTENT_PROVIDER=gemini` with rule fallback on missing key/errors, optional Gemini env config, and a supervisor greeting agent so simple greetings like `hello` return a friendly Sentient prompt instead of the clarification question; verification passed for AI Agentic type-check and focused classifier/LangGraph/draft-routing tests, while full build remains blocked by the live Windows Prisma query-engine DLL lock.
- [codex] ai-conversation-detail-message-mapper: Fixed `GET /api/ai/conversations/:id` returning 500 for conversations with messages by making `ConversationResponseMapper.toMessage` independent of unbound static-method `this` context and adding regression coverage for persisted detail message mapping; verification passed for focused AI conversation management tests and AI Agentic type-check, while full build was blocked by the currently running dev process locking Prisma's Windows query-engine DLL.
- [codex] ai-agentic-prisma-dev-dist-copy: Fixed the AI Agentic dev/runtime `Cannot find module '../../generated/prisma'` crash by adding Prisma generate hooks before dev/build and configuring Nest to copy `src/generated/prisma` into `dist`; verification passed for AI Agentic type-check, build, and a direct built-service import smoke.
- [codex] 016-ai-module-langgraph-architecture: Adopted the recommended AI architecture by adding `@langchain/langgraph`, introducing `SupervisorLangGraphRunnerService` as the compiled LangGraph.js workflow for supervisor intake, conditional routing, clarification, specialist execution, human escalation, and final answer nodes, reducing `SupervisorAgentService` to a stable NestJS facade, preserving existing specialist/audit/permission services as graph node dependencies, adding Jest `uuid` mapping needed for LangGraph's CJS test runtime, documenting the architecture in the AI module plan/state-machine contract, and verifying AI Agentic type-check, tests, and build.
- [codex] 016-ai-module-review-fixes: Closed skeptical review gaps in the AI module by threading resumed conversation context into supervisor/specialist inputs, defaulting conversation lists to active records and blocking archived follow-up sends, hardening title/preview redaction for sensitive/person data, tightening Sentient-only and interpersonal-judgment guardrails, wiring scoped HR Core/Social lookups into Leave/OKR/Career/Analytics/Onboarding/General Help agents with downstream permission decisions, recording specialist source categories/permission decisions in audit logs, adding context-source health probes, implementing governance `from`/`to`/`agentType` filters, and expanding guardrail/conversation/governance/resume tests; verification passed for AI Agentic type-check, test, and build.
- [codex] 016-ai-module-implement: Implemented the AI module scaffold through US3 with shared AI enums, Prisma `ai_agent` persistence/migrations, real Prisma client, public AI health roster, guarded conversation/feedback/governance APIs, deterministic supervisor state machine with OKR/Career/Analytics/Onboarding/Leave/Language/General Help/Human Escalation specialists, Sentient-only/off-topic/interpersonal-judgment guardrails, conversation list/detail/archive/delete/resume support, draft-safe read-only policy and copy toolbar, Web AI Assistant route/sidebar entry, and gateway AI health/conversation proxy coverage; verification passed for AI type-check/test/build, Web type-check, and Gateway tests on rerun, with full live multi-service quickstart smoke still pending because local dev servers were not running.
- [codex] 016-ai-module-tasks: Generated Spec Kit implementation tasks for the AI module scaffold in `specs/016-ai-module/tasks.md` with 98 dependency-ordered checklist items across setup, foundation, US1 supervisor-agent entry point, US2 conversation continuity/feedback/governance, US3 safe HR drafting, and final verification; format validation passed (`98` task rows, `0` invalid rows).
- [codex] 016-ai-module-plan: Completed Spec Kit planning artifacts for the AI module scaffold on `016-ai-module`, covering a read-only supervisor-agent state machine, named domain sub-agents (OKR, Career, Analytics, Onboarding, Leave, Language, General Help, Human Escalation), clarification/final-answer nodes, conversation/audit/escalation/RAG data model, `/api/ai/conversations` contracts, quickstart, Sentient-only polite scope handling, and interpersonal-judgment escalation guardrails.
- [codex] home-approved-leave-format-shared: Fixed the Home page Recently Approved Leave rows to use formatted leave dates instead of raw ISO timestamps by adding a shared `LeaveRequestSummary` React component and reusing it in both Home and Leaves My Requests/Team Requests tables; verification passed for Web type-check.
- [codex] performance-review-details-action-polish: Modernized the Performance Reviews table Details action into a professional app-native primary-tint outline button with a compact icon badge, polished hover state, and accessible per-review labeling; verification passed for Web type-check.
- [claude] 017-action-execution-and-telegram-ai: Implemented the spec 017 leave-booking action engine (T022-T058 except the three named integration-spec files T025/T037/T053, and T031 in a best-effort form: policy is retrieved via `KnowledgeRepository.searchApproved()` without a region filter because `regulationRegion` does not exist) and wired the Telegram bot to the real agent pipeline. Reason/Propose: `LeaveBookingReasonService` runs before the LLM tool-caller in `LeaveAgentService`; deterministic `leave-booking-request.parser` extracts dates/type; identity first, leave types scoped to the business unit, advisory business-day count from the holiday calendar (never compared to HR Core's), balance+overlap from one `getLeaveContext`, declines with concrete numbers, one clarifying question rather than a guess. `ActionProposalService.mint()` runs in `ConversationsService.executeTurn` AFTER the assistant message exists (messageId is a non-null unique FK — T032 as written was unimplementable) and only when the REVIEWED status is still PENDING_CONFIRMATION, so a policy-swept refusal never ships a live token; it writes `action.proposed` first and stores the log id on the row. Confirm/Cancel: `ActionProposalService.decide()` sequences look-up -> scope check -> re-validate -> consume (conditional updateMany, writes CANCEL/EXECUTE intent atomically) -> `ActionExecutorService.execute` (one write, frozen payload, FR-007 failure classes verbatim) -> verify (read-back on type/dates/employee/status, NEVER totalDays) -> `action.executed`/`action.verified` linked via parentLogId. A confirm turn persists NO user message (T047) so `ConversationTurnResponse.userMessage` is nullable; the turn carries `actionOutcome`. `confirmation-card.presenter.ts` is the single source of the wire `ConfirmationPayload`, `ActionOutcome` and card/outcome text for both web (`action-confirmation-card.tsx`, wired in `ai-assistant.tsx`) and Telegram. Telegram: `ActorContextFactory.fromChannelToken()` VERIFIES the exchanged JWT (shared JWT_SECRET), asserts channel + `AI_USER_ROLES` (moved to `common/graph/ai-user-roles.ts`); new `ai_agent.channel_conversation_links` table (migration 20260913000000) maps a chat to its ongoing conversation, `/new` clears it; inline Confirm/Cancel buttons carry `c:<uuid>`/`k:<uuid>` callback_data, the handler answers the callback first, routes by proposal token (not the chat link), strips the keyboard, then calls the same `sendMessage` path as web. Re-established every HTTP-only guard the bot bypasses (role gate, length cap, requestTimeoutMs race that defers to the web app, per-chat serialization, 4096-char chunking, no parse_mode, Jest guard, 120s token margin, 404-vs-503 in resolveSession). Also fixed a real bug from the previous merge: `ChannelIdentitiesService.exchange()` never revoked the prior session, so every exchange after the first 500ed on `sessions_active_channel_uidx`; and added the missing hr-core migration `20260911000000_add_channel_identities`. Verified over HTTP and in the browser: propose writes nothing; two concurrent confirms create exactly one leave_request; verify MATCHED; cancel leaves executed_at null; audit chain linked. ai-agentic suite: 421 passed. NOT done: US4 compassionate sick variant beyond default-to-today (T059-T069), US5 proactive follow-up (T070-T082), US6 extra audit tests (T083-T088), T089-T091 docs. Known cosmetic: `previewFrom` redaction masks "Annual Leave" as "[person]" in conversation previews.
- [claude] telegram-channel-identities: Implemented free Telegram bot integration (long polling via grammY — no public URL/domain needed) with a new HR Core `channel-identities` module: Prisma models `ChannelIdentity`/`ChannelLinkCode` + `TELEGRAM` added to the `ChannelType` enum + 2 new `SecurityEventType` values (migration `20260911000000_add_channel_identities`); a 3-step handshake (web app generates a one-time 6-digit code → bot redeems it SYSTEM-authenticated → bot exchanges the linked externalId for a real, RBAC-scoped session, never a SYSTEM/GLOBAL bypass); new shared `SystemTaskGuard`/`@RequireTaskType()` in `@sentient/shared` so a SYSTEM JWT is scoped to one task (`channel_link` / `channel_token_exchange`), reusable by future SYSTEM-JWT consumers (exit-survey dispatch, engagement snapshots); fixed a pre-existing bug in `UserStatusGuard` that rejected every SYSTEM JWT (it looked up a `User` row for `sub: 'system'`, which never exists) — this was blocking the SYSTEM JWT mechanism entirely, not just this feature; `employee.terminated` now revokes `ChannelIdentity` + SLACK/TELEGRAM sessions (does not revoke WEB sessions — that broader behavior doesn't exist anywhere yet); ai-agentic gets a new `channels/telegram` module + minimal `HrCoreClient` (mints its own short-lived SYSTEM JWT per call, no incoming user JWT to forward), gated behind `TELEGRAM_ENABLED` so hot-reload in dev doesn't fight a second long-poller (Telegram allows one `getUpdates` poller per token). Deliberately scoped to identity linking + transport only — no `Conversation`/`Message`/agent pipeline; free-text messages get a placeholder reply until the HR Assistant agent lands, which will plug into the same `resolveSession()` seam. Verification: `packages/shared`, `hr-core`, `ai-agentic` all type-check clean; new `channel-identities.service.spec.ts` (11 tests, including the terminated-account exchange rejection) passes; `prisma validate` passes; pre-existing hr-core test/type-check failures (Prisma 5→7 generated-client `runtime/library` path mismatch, unrelated org-module test stubs) confirmed unrelated via before/after diff.
- [codex] social-events-emoji-reactions: Added Social event publishing and emoji reactions end-to-end with a new `event_reactions` table, Events NestJS module (`POST/GET /events`, `PUT /events/:id/reaction`), best-effort `event.created` domain emission, organizer enrichment, web Social API helpers, an Events page with publish dialog and per-user reaction toggles, and sidebar/route wiring; local migration applied; verification passed for Social/Web type-check, Social tests, and authenticated gateway `GET /api/social/events` smoke.
- [codex] api-gateway-dev-jwt-alignment: Fixed local gateway 401s for authenticated Social routes such as `POST /api/social/announcements` by aligning the API Gateway no-env JWT fallback with the shared HR Core/Social local development secret; added gateway config regression coverage for gateway-specific, shared, and default JWT secret precedence; verification passed for gateway type-check and full gateway test suite.
- [codex] 015-api-gateway-review-fixes: Closed review gaps against `specs/015-api-gateway/spec.md` by allowing gateway-validated SSE `accessToken` query auth for HR notification streams, wrapping all non-2xx upstream responses including redirects, rendering `/api/docs` as a grouped browsable service docs page, adding downstream target logging, moving frontend gateway error code/message handling into one shared helper, switching public IP throttling to Express trusted-proxy resolution, adding `API_GATEWAY_TRUST_PROXY`, fixing the quickstart signin route to `/api/hr/auth/login`, and expanding gateway tests for invalid JWT signatures, SSE auth, public/override rate limits, 413/502/504, redirect envelopes, docs HTML grouping, and request-log fields; verification passed for gateway type-check/test/build and web type-check, with live multi-service quickstart smoke T077 still pending.
- [codex] 015-api-gateway-implement: Implemented the NestJS API Gateway service in `apps/api-gateway` with typed config, central JWT validation, public route allow-list, streaming HR/Social/AI proxying, correlation ids, in-memory edge throttling, standard gateway/upstream error envelopes, aggregate health/docs endpoints, local upstream e2e helpers, Docker/env/docs wiring, and frontend migration to one `/api` gateway origin; verification passed for gateway type-check/test/build and web type-check, with live multi-service quickstart smoke still pending running downstream services together.
- [codex] 015-api-gateway-tasks: Generated Spec Kit implementation tasks for API Gateway in `specs/015-api-gateway/tasks.md` with 77 dependency-ordered checklist items across setup, foundation, US1 single entry-point routing/frontend migration, US2 central JWT validation, US3 correlation, US4 edge rate limiting, US5 standard error envelopes, US6 health/docs aggregation, and final packaging/verification; format validation passed (`77` task rows, `0` invalid rows).
- [codex] 015-api-gateway-plan: Continued Claude's API Gateway feature from `specs/015-api-gateway/spec.md`; completed Spec Kit planning artifacts (`plan.md`, `research.md`, `data-model.md`, `quickstart.md`, `contracts/gateway-api.yaml`) for a stateless NestJS gateway with streaming proxy routing, central JWT validation, correlation ids, in-memory edge rate limiting, standard error envelope handling, health/docs aggregation, and a one-base-URL frontend migration.
- [claude] 014-documents-module: Documents module implemented end-to-end in Social — Prisma additive migration (`isPublic` flag + `(isPublic, createdAt desc)` index), `DocumentStorage` interface with `FilesystemDocumentStorage` implementation (path-traversal guard, ENOENT-tolerant delete, `StorageWriteError`/`StorageKeyNotFound`), `mime-to-extension` + `sanitizeFilename` helpers, 3 DTOs (Create/Update/ListQuery with multipart-string `isPublic` transforms), `DocumentsService` (create/findAll/findOne/download/update/remove with visibility 404-not-403, SYSTEM-JWT download bypass, version-bump only on file replace, best-effort storage cleanup, EventBus failure non-blocking, uploader enrichment with de-duplication, `prisma.$transaction([findMany, count])`), `DocumentsController` (6 endpoints + multer file-size filter translating `LIMIT_FILE_SIZE` → `FileTooLarge` 400, full Swagger), `document.uploaded` event from POST + version-bumping PATCH, `document.deleted` event from DELETE; frontend: `social.ts` extended with full documents API surface + `DOCUMENT_ERROR_MESSAGES` mapping FR-031 codes, new `documents.tsx` page (debounced search 300ms, category chips, role-gated upload/edit/delete, hidden-anchor download), route + nav entry; service unit tests + filesystem-storage unit tests + integration test (gated on `SOCIAL_INTEGRATION_DB=1`); `DocumentCategory` shared enum corrected to spec values (INTERNAL_POLICY/HANDBOOK/REGULATION/TEMPLATE/GUIDE/OTHER). Social `pnpm build` clean.
- announcement-dev-proxy-routing: Web announcement API calls now default to `/api/social` and Vite proxies that prefix to the Social service on port 3002 with dedicated `[proxy:social]` terminal logs; Social now runs Prisma generate before dev/build and copies `src/generated/prisma` into `dist`, fixing the missing generated client crash that caused `ECONNREFUSED`; announcement controller methods now forward the caller JWT into HR Core enrichment so create/list/detail/update/pin no longer fail with a post-write `401`.
- [claude] 013-announcements-module: Full announcements module implemented — Prisma schema migration (targetDepartmentId, targetTeamId, expiresAt + 2 indexes), DepartmentRef/TeamRef interfaces, HrCoreClient extended with getDepartmentRef/getTeamRef (TTL-cache), AnnouncementsModule wired into AppModule, all 5 DTOs (create/update/list/pin), AnnouncementsService (6 methods: create, findAll, findOne, update, remove, pin) with audience-filter logic, expiry filtering, FR-027 error codes, best-effort event emission, author enrichment de-duplication, AnnouncementsController (6 endpoints with full Swagger), 25-test unit suite, integration test (gated on SOCIAL_INTEGRATION_DB=1), contract tests; frontend: social.ts API client with 6 typed functions + extractApiError + error-code map, announcements.tsx page (pinned badges, role-gated publish/edit/pin/delete), route in App.tsx, nav item in layout.tsx; T030 (curl smoke-test) still pending running services
- workforce-dashboard-demographics: Dashboard Employees tab now adds PowerBI/Tableau-style workforce composition charts for education level, education field, total employees by gender, attrition by marital status, and attrition by job; HR Core analytics now returns those slices and the employee model/seed/create/edit flows include gender.
- risk-threshold-dashboard-alerts: Dashboard KPI cards now render warning/critical threshold breaches with stronger orange/red instrument-panel styling plus an overview alert deck, while threshold settings/backend validation reject negative or misordered warning/critical values.
- [claude] 012-social-scaffold: Social scaffold landed — Prisma 8-entity schema (announcements, events, event_attendees, documents, feedback, engagement_snapshots, exit_surveys, exit_survey_responses) + 8 Postgres enums + 10 indexes + migration, 4 new @sentient/shared enums (Audience/RsvpStatus/SentimentLabel/FeedbackType), SharedJwtGuard+RbacGuard global APP_GUARDs, @Public() on /health, HrCoreClient with in-process 60s employee-ref cache, local InMemoryEventBus/EventBusModule mirror, env templates, full test suite (auth-wiring, event-bus, hr-core.client, e2e health) all green
- performance-review-detail-modern-ui: Refined the Performance Review Details dialog into a modern scan-first review dossier with a dark summary band, status/rating chips, self-vs-manager rating panels, satisfaction snapshot, narrative cards, and timeline markers.
- performance-review-detail-dialog: Performance Reviews now exposes a Details action on every visible review row so employees, managers, and HR can inspect self/manager ratings, satisfaction scores, comments, org context, timestamps, rating gaps, and salary follow-up counts without needing an editable review state.
- org-chart-business-unit-filter: Org Chart now exposes Business Unit filters ahead of department filters, scopes department tabs and visible branches to the selected BU, shows BU names on department cards, and includes BU metadata in the HR Core org-chart response.
- org-chart-replit-behavior-port: Org Chart now uses the front-replit hierarchy behavior against live HR Core data, including CEO root support, skill search/highlights/tooltips, recent-joiner badges, department filters, and collapsed team branches that expand from lead-only views.
- okr-cascade-tree-expand-collapse: OKR Cycle Management now renders the cascade as a friendlier expandable tree, with per-parent expand/reduce controls, global Expand all/Reduce all actions, child counts, and clearer Company/Department/Employee visual hierarchy.
- okr-objectives-table-cascade-levels: The selected-cycle Objectives table now renders objectives in cascade order with indentation, explicit Company/Department/Employee badges, and parent alignment context instead of a flat level column.
- okr-cascade-management-ui: OKR Management now shows an explicit Company → Department → Employee cascade for the selected cycle, including active company objectives from a quarterly cycle's annual parent; managers can start a department objective directly under a company objective and activate linked draft department objectives from the cascade.
- okr-management-manager-tier-nav: OKR Management is visible to both department-manager and team-lead frontend tiers, because MANAGER JWTs can be classified by assignment scope; the route remains role-tier gated and backend OKR RBAC still enforces actual objective permissions.
- okr-manager-nav-access: Department managers now see an OKR Management sidebar entry that opens the existing cycle/objective management page, and the route is explicitly gated to HR admins and department managers so managers can activate their draft department objectives without exposing HR-only cycle administration.
- okr-employee-objective-owner-flow: Employee-level OKR creation now uses a scoped employee owner picker instead of raw IDs, auto-assigns self-service employee objectives to the current user, normalizes selected Employee IDs to linked User IDs in HR Core, and clarifies that active department objectives are required before employee OKRs can align.
- okr-manager-department-objectives: Department managers now create department OKRs without entering raw department IDs; HR Core derives the manager department from JWT claims, the OKR cycle page opens manager objective creation at Department level, and selected-cycle objectives can be activated from the UI so active parent objectives become selectable.
- okr-parent-objective-dropdown: Fixed the Objective form parent dropdown so department OKRs in quarterly cycles can align to active company objectives from the parent annual cycle, with loading/empty states instead of a blank menu.
- okr-cycle-date-normalization: Fixed OKR cycle creation so date-only UI payloads are normalized to UTC Date objects before Prisma DateTime writes, and cleaned the OKR RBAC test fixture so HR Core type-check remains green.
- 011-okr-module: OKR module — 4 new entities + 7 enums in hr_core, 3-level cascading hierarchy (Company/Department/Employee), dual-track contribution (personal OKRs auto-approve + shared KR check-ins via Manager review), notifications integration via okr.rules.ts + new OKR enum values, 14-day reminder cron (OkrReminderScheduler), 3 React pages (okr-dashboard, okr-cycle-management, my-okrs), HR Core backend clean build
- promotion-position-catalog-alignment: Promotion requests now require selecting an active Position by job-family in Simulation, removed custom promoted-role titles, and HR Core derives current/new role titles from canonical positions while applying the promoted position on approval.
- positions-domain-sections: Reorganized the Positions page into seeded job-family sections (Engineering, Product & Design, HR, Finance, Sales) and grouped each position's required skills by actual SkillDomain, with backend position-skill domain filtering support.
- employee-profile-gap-radar-port: Reworked the main React employee profile Skills tab to use a front-replit-style Skills & Proficiency gap radar, overlaying employee proficiency against role requirements and listing partial/missing gaps below the chart.
- front-replit-skill-gap-fallback: Employee profile skill gap information now renders in the isolated Replit frontend even when the local `/api` server is offline, using exported mock employee positions and required skills as a client-side fallback.
- employee-self-profile-nav: Added a My Profile nav entry and self-service profile route for every linked employee, hid HR-only edit controls outside HR admin views, exposed own salary history safely, and added scoped promotion history to employee profile details.
- simulation-hr-review-actions: Added HR admin Validate/Refuse actions to pending promotion request cards in the Simulation section, reusing HR Core approve/reject endpoints with query invalidation and toast feedback.
- promotion-submit-validation-scope: Fixed Simulation promotion submission by aligning manager create-scope with department/team leadership visibility, adding frontend validation for salary increases and required responsibilities, and surfacing HR Core validation messages instead of the generic scope error.
- simulation-budget-decimal-normalization: Fixed Simulation team-budget math by normalizing employee gross/net salary Decimal strings to numbers in the HR Core web API client, added a defensive salary coercion in the promotion wizard, and verified the live pending Backend promotion row still stores a realistic 367,918.00 team budget.
- promotion-simulation-budget-review: Promotion creation now computes current salary/team budget from HR Core instead of trusting frontend values, Simulation requests scoped compensation data only for HR/manager-owned employees, HR admins get a visible Pending HR Decisions card with Validate/Refuse actions, and the local bad Backend pending request was backfilled from real salary data.
- promotion-review-compensation: Added HR admin Validate/Refuse actions to the dashboard Promotions tab, wired them to HR Core approve/reject endpoints, made approval apply the promoted gross/net salary with salary-history audit, and filled canonical seed/live local salary history with realistic net salary movement.
- manager-simulation-performance-access: Opened Simulation to manager tiers and scopes its employee/request data to the manager's department/team before rendering promotion candidates; Performance Reviews now appears for every role while HR-only cycle actions remain HR-only.
- hrbp-dashboard-analytics: Added scoped HRBP workforce analytics to the dashboard API and React dashboard, including probation/exits, average age, average tenure, full-time ratio, exit rate, status breakdown, contract mix, age bands, and tenure bands.
- dashboard-analytics-trust: Fixed scoped dashboard employee stats so Total Employees counts all non-deleted people in scope while Active/On Leave stay status-specific; current payroll, skills, salary history, and pending approval queries now exclude terminal employees, with analytics unit coverage for department-manager scope.
- canonical-hr-seed: Consolidated HR Core seeding into `apps/hr-core/prisma/seed.ts`; removed the divergent `seed-replit.ts` and generated seed JS artifacts; Prisma config and package scripts now point to the canonical TypeScript seed, with Replit/performance-review enum metadata preserved in `seed.ts`.
- manager-scoped-employee-details: Department managers can open employee profiles for employees in their department, team leads can open profiles for employees in their team, HR still sees all profiles, and regular employees keep list-only access.
- role-scoped-directory-dashboard: Team leads and department managers now see Dashboard with team/dept-scoped analytics, including demo manager tokens that lack explicit scoped role assignments; org chart and org filter data are available to all authenticated users; employee directory is browseable by everyone while profile detail entry points are HR-only and non-HR API detail access is limited to self.
- employee-profile-business-unit: Fixed employee profile Professional Details so Business Unit is resolved from the employee API payload instead of staying in a Loading state; employee API now includes department/team Business Unit refs.
- employee-data-standardization: Cleaned live HR Core employee profiles so all active full names are unique, DOB/marital/phone/education/net salary fields are populated, employee emails/phones are standardized, and future reseeds use the same realistic unique profile generation with an active-name uniqueness index.
- employee-directory-org-context: Added BU-aware employee directory context so department groups and rows show the parent business unit; employee profile header/details now show Business Unit, and profile department selection labels duplicate department names with their BU path.
- 010-notifications: generic in-app notification module — Notification table + 3 enums in hr_core, event-bus subscriber wiring all domain events (leave + promotion live; skills/perf/probation/contract/complaint/engagement/exit-survey wired-but-inactive), bell + drawer in apps/web, SSE realtime with TanStack cache merge + 30s polling fallback, role-aware notification deep links, 90-day retention
- promotion-dashboard: Added durable HR Core promotion requests with Prisma model/migration, shared status enum, guarded NestJS create/list/dashboard endpoints, and service tests; moved Simulation promotion submissions from localStorage to live HR Core employees/API; updated Dashboard Promotions tab with year + org-scope filters, total requests, average salary lift, total budget impact, pending requests, and request detail table.
- skills-domain-front: Aligned web required-skills API types with the backend PositionSkill DTO (`skillId`, `minimumProficiency`, optional `requirementLevel`, bulk `{ skills: [...] }` payload); routed legacy positions-api helpers through the HR Core client; added position required-skills filtering by SkillDomain and visible requirement-tier counts/labels, plus backend domain filtering in the add-skill picker; updated employee-profile gap radar to render skills grouped per domain and the gap card to list actual Mandatory/Expected/Nice-to-Have skills with required/acquired proficiency.
- skills-domain: Added SkillDomain enum (TECHNICAL/LEADERSHIP/SOFT_SKILLS/DOMAIN_EXPERTISE) to shared pkg + Prisma schema; wired domain field into catalog DTOs, position-skills, employee-skills, history, and gap analysis services; added position required skills CRUD panel to positions.tsx (expandable per row, add/delete dialog, HR_ADMIN gated); replaced flat SkillsRadarCard with GapRadarCard (required vs acquired overlay) on employee-profile.tsx; enhanced SkillsGapCard with By Domain / By Requirement Level / All Skills tabs; seeded skill domains in seed.ts; migration at 20260510000000_add_skill_domain; run `prisma migrate dev` to apply
- 009-coordination: Established Claude Code ↔ Codex session-start protocol and fixed .claude/rules path reference in AGENTS.md
- 009-performance-review: Planned HR Core performance review cycles, self-review, manager review, HR outcome tracking, and API-backed web workflow using the existing TypeScript/NestJS/Prisma/React stack
- 005-leave-module: Added TypeScript 5.x (strict mode) + NestJS 10, Prisma 5 (multiSchema), class-validator, class-transformer, @nestjs/swagger, @nestjs/schedule (for monthly accrual cron), @nestjs/config
- 004-skills-module: Added TypeScript 5.x (strict mode) + NestJS 10, Prisma 5 (multiSchema), class-validator, class-transformer, @nestjs/swagger, @nestjs/config
- 003-employee-module: Added TypeScript 5.x (strict mode) + NestJS 10, Prisma 5 (multiSchema), class-validator, class-transformer, @nestjs/swagger, @nestjs/config


## Agent Coordination Protocol

This project uses **two agents in sequence**: Claude Code and Codex. You never run at the
same time. The shared source of truth is **git history** and **`specs/*/tasks.md`**.

### At Every Session Start — Do These Before Writing Any Code

1. Run `git log --oneline -10` to see what the other agent last did and which branch is active.
2. Identify the active feature branch (e.g., `009-performance-review`) and read
   `specs/<feature>/tasks.md` to know which tasks are done (`[x]`) and which remain (`[ ]`).
3. Read `AGENTS.md` Recent Changes for the last feature that was added.

### Commit Message Convention

Prefix every commit with `[codex]` so the git log clearly shows which agent produced each change:

```
[codex] feat(performance-review): implement ReviewCyclesService create/initiate
[codex] fix(shared): correct PerformanceRating enum export
```

Claude Code uses `[claude]` for the same reason. This makes `git log --oneline` a readable
cross-agent work log without any extra tooling.

### When You Finish a Session

Update the `## Recent Changes` section at the top of this file with what you implemented,
using the same format as existing entries. This is the handoff note for the next agent.

### What You Do NOT Need to Do

- No separate state file to maintain — git is the state.
- No task attribution in `tasks.md` beyond the `[x]` checkbox — git blame covers attribution.
- No synchronization file — sequential execution means no collision risk.

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->

